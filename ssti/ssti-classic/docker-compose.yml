version: "3.8"

services:
  python-flask:
    container_name: ssti-classic-6
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1337:5000"   # host:container
      - "5678:5678"   # optional debugpy
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
